---
id: autocad-autopage-plugin
title: 逃離「頁碼地獄」：我如何用 AutoLISP 寫出「自動頁碼」插件，徹底告別手動修改！
category: 技術分享
date: 2026.07.14
summary: 在室內設計繪製施工圖的過程中，頻繁調整配置分頁順序是常有的事。然而，手動逐頁修改圖框裡的頁碼（PAGE_NO）卻是個極度消磨心智的繁瑣工作。本文記錄了我如何透過 AutoLISP 腳本 (AutoPage.lsp)，利用 AutoCAD 的 TabOrder 物理順序自動排序，實現一鍵秒級批量更新所有配置頁碼的自動化歷程。
image: https://images.unsplash.com/photo-1581092335397-9583fe92d232?auto=format&fit=crop&w=800&q=80
---

在室內設計師的日常裡，繪製一整套完整的施工圖——從拆除、平面、天花板到各個立面細部折圖——就像是用線條蓋一棟精緻的房子。

然而，當圖面畫完，最讓人感到「心智磨損」的，往往不是設計本身的難題，而是那些看似微小卻極其繁瑣的庶務。其中最具代表性的，就是**修改圖紙頁碼**。

---

### 一、 隱藏在施工圖背後：那場名為「頁碼」的重覆性夢魘

想像一下這個再熟悉不過的場景：
你辛辛苦苦畫完了 30 張配置圖，好不容易把圖框裡的頁碼從 `1` 改到 `30`。這時，業主突然決定在天花板跟燈具迴路圖之間，再塞一張空調配置圖，或者主管臨時要求把立面圖的順序前後對調。

這意味著：
1. 你必須手動切換到每一個配置分頁（Layout）。
2. 對著圖框右下角的頁碼點擊兩下，喚醒屬性編輯器。
3. 把數值加一、儲存、然後切換到下一個分頁……重複這個動作 30 次。

這不只是浪費時間，更是一大心理阻力。只要手一抖、漏改一張，出圖時就會出現頁碼跳號或重複的低級錯誤，對專業度大打折扣。

「這件事情，為什麼不能讓電腦自己算、自己填呢？」

為了消滅這個高摩擦力的低價值勞動，我決定寫一個專屬的 AutoCAD 插件——**自動頁碼更新工具**。

---

### 二、 設計邏輯：讓程式像設計師一樣「看懂」分頁順序

要讓 AutoCAD 自動更新頁碼，程式必須做到兩件事：
1. **知道目前有幾個配置，而且知道它們實體的左右排列順序**。
2. **抓到圖紙右下角的頁碼欄位，並將正確的號碼寫進去**。

經過研究，我選擇了 AutoCAD 最老牌也最直接的腳本語言 **AutoLISP** 來開發，並寫下了 `AutoPage.lsp`。以下是它的核心邏輯與技術亮點：

- **以 `TabOrder` 作為真實的物理排序依歸**  
  在 AutoCAD 的資料結構中，配置分頁的順序是由屬性 `TabOrder` 決定的。程式啟動後，會先透過 ActiveX/COM 介面 (`vl-load-com`) 撈出所有配置，自動排除「Model（模型空間）」，並依照 `TabOrder` 的數字從小到大進行升冪排序。這樣一來，無論我們把分頁拖移到左邊還是右邊，程式讀取到的永遠是畫面上最真實的物理排列順序。

- **動態鎖定「`PAGE_NO`」屬性標籤**  
  一般圖框通常是由「屬性塊（Attribute Block）」組成的。我將頁碼的屬性標籤（Tag）統一定義為 **`PAGE_NO`**。LISP 程式會用選擇集過濾出每個配置中的屬性塊，一旦比對到 `PAGE_NO`，就利用 `entmod` 和 `entupd` 函數將當前順序的累加值寫入，實現無感的一鍵刷新。

---

### 三、 成果與體驗：一鍵執行，秒級搞定

現在，我的軟體工作流多了一個極具儀式感且無比輕盈的動作：
當我增刪或重新調整了配置順序後，只要在 AutoCAD 命令行中輸入：

```text
UpdatePage
```

畫面會快速閃爍、依序切換分頁，僅僅幾秒鐘的時間，終端機便會回傳：
> **「全部頁碼已依照標籤順序更新完成！」**

那些過去需要花上十分鐘、甚至容易改錯的繁瑣微調，在寫好腳本後，變成了一個簡單的指令。

---

### 四、 結語：消滅摩擦力，把專注力留給設計本身

身為一名空間設計師，我深信「好設計的本質在於解決問題，並帶來秩序與美感」。而對待自己的工作流程，也應該是一樣的態度。

這段「自動頁碼」的開發故事，是我持續優化軟體工作流的一個小小註腳。將這種機械式的重覆動作自動化，不僅能大幅降低心靈的摩擦力，更能讓我們把珍貴的專注力，百分之百投注在真正有創造力的空間設計上。

***

> **工具路徑與原始碼存檔**：
> - 腳本檔案：[AutoPage.lsp](file:///G:/%E6%88%91%E7%9A%84%E9%9B%B2%E7%AB%AF%E7%A1%AC%E7%A2%9F/Markdown%E5%84%B2%E5%AD%98%E5%BA%AB/02_Areas%20(%E4%BD%A0%E7%9A%84%E9%95%B7%E6%9C%9F%E6%8A%80%E8%83%BD%E8%88%87%E8%BA%AB%E5%88%86)/%E5%AE%A4%E5%85%A7%E8%A8%AD%E8%A8%88/%E8%BB%9F%E9%AB%94%E5%B7%A5%E4%BD%9C%E6%B5%81/%E8%87%AA%E5%8B%95%E9%A0%81%E7%A2%BC/AutoPage.lsp)
> - 使用說明書：[安裝後使用指令 UpdatePage.txt](file:///G:/%E6%88%91%E7%9A%84%E9%9B%B2%E7%AB%AF%E7%A1%AC%E7%A2%9F/Markdown%E5%84%B2%E5%AD%98%E5%BA%AB/02_Areas%20(%E4%BD%A0%E7%9A%84%E9%95%B7%E6%9C%9F%E6%8A%80%E8%83%BD%E8%88%87%E8%BA%AB%E5%88%86)/%E5%AE%A4%E5%85%A7%E8%A8%AD%E8%A8%88/%E8%BB%9F%E9%AB%94%E5%B7%A5%E4%BD%9C%E6%B5%81/%E8%87%AA%E5%8B%95%E9%A0%81%E7%A2%BC/%E5%AE%89%E8%A3%9D%E5%BE%8C%E4%BD%BF%E7%94%A8%E6%8C%87%E4%BB%A4%20UpdatePage.txt)
